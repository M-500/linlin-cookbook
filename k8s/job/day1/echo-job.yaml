apiVersion: batch/v1
kind: Job
metadata:
  name: echo-job

spec:
  template:
    spec:
      containers:
      - name: echo-box
        image: busybox
        imagePullPolicy: IfNotPresent
        command: [ "/bin/echo" ]
        args: [ "Hello","World" ]